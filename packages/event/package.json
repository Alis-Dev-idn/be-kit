{
  "name": "@alisdev/be-kit-event",
  "version": "2.0.0",
  "private": true,
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "eventemitter2": "^6.4.9",
    "reflect-metadata": "^0.2.x"
  },
  "peerDependencies": {
    "redis": "^4.x",
    "bullmq": "^5.x"
  }
}
