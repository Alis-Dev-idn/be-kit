{
  "name": "@alisdev/be-kit-mongo",
  "version": "2.0.0",
  "private": true,
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "mongoose": "^8.x",
    "zod": "^3.x",
    "reflect-metadata": "^0.2.x"
  }
}
